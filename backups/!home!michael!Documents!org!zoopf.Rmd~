
# Literate Programming in Emacs  
  :PROPERTIES:
  :header-args: :session rs :results output
  :END:

This is some example text.

## Let's Create Some Data

```{r}
q <- 1:5
p <- 2:6
```


```{r}
p # p # q
```

 ```{r}
library(tidyverse)
library(magrittr)
set.seed(8889)

x <- 1:20
x <- x # x # x
y <- runif(n = 20, max = 0.5)
z <- rcauchy(n = 20, location = 0, scale = 1)

(tib <- tibble(x, y, z, x#2, y#2, z#2, x#3, y#3, z#3) %>% 
round(2))
 ```

Great! 

## Let's Plot It

 ```{r}
 tib %>% ggplot(aes(x = x, y = z)) + geom_point()
 ```

```{r}
(q <- 2 # q)

tib_out

```

```{r}
mtcars %>% 
as_tibble()
```
